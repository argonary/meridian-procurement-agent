# -- Required for Tier 1 (SQLite) --------------------------
ANTHROPIC_API_KEY=sk-ant-...

# -- Required for Tier 2 (Databricks) ----------------------
# Add these only after Tier 1 is fully working
DATABRICKS_SERVER_HOSTNAME=your-workspace.azuredatabricks.net
DATABRICKS_HTTP_PATH=/sql/1.0/warehouses/your-id
DATABRICKS_TOKEN=dapi...
DB_CATALOG=meridian
DB_SCHEMA=procurement
